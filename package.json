{
  "name": "apex-dev",
  "version": "3.10.44",
  "description": "Apex AI - a friendly agentic coding assistant for the terminal",
  "main": "dist/index.js",
  "bin": {
    "apex": "./cli.js",
    "apex-dev": "./cli.js"
  },
  "files": [
    "dist/index.js",
    "cli.js"
  ],
  "scripts": {
    "build": "bun scripts/build-entry.mjs && NODE_ENV=production bun build entry.mjs --target=bun --outfile=dist/index.js --packages=external",
    "prepack": "npm run build",
    "start": "bun apex.mjs",
    "test": "bunx repterm tests/",
    "test:record": "bunx repterm --record tests/",
    "test:verbose": "bunx repterm -v tests/",
    "lint": "find cli.js apex.mjs entry.mjs src scripts tests -type f \\( -name '*.js' -o -name '*.mjs' -o -name '*.ts' \\) -print0 | xargs -0 -n1 node --check && bun build index.jsx --target=bun --outfile=/tmp/apex-dev-lint-check.js --packages=external >/dev/null && rm -f /tmp/apex-dev-lint-check.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@opentui/core": "^0.1.83",
    "@opentui/react": "^0.1.83",
    "openai": "^6.25.0",
    "react": "^19.2.4"
  },
  "devDependencies": {
    "repterm": "^0.2.1"
  }
}
